import React from 'react'
import news from './News.module.css'

const News = (props) => {
    return (
        <div className={news.wrapper}>
        <h2 className={news.section_title}>This block under reconstruction</h2>
        </div>
    )
}

export default News;